#include "src/menu.h"
#include <iostream>
#include <vector>

using namespace std;
using namespace cv;
using namespace mondongo;

int main() {
    cv::utils::logging::setLogLevel(cv::utils::logging::LogLevel::LOG_LEVEL_SILENT);
    start();
    return 0;
}